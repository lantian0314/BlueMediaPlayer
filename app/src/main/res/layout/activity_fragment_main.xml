<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <!--标题栏-->
    <include layout="@layout/titlebar"/>

    <android.support.v4.view.ViewPager
        android:id="@+id/vPager"
        android:layout_width="fill_parent"
        android:layout_height="0dp"
        android:layout_gravity="center"
        android:layout_weight="1.0"
        android:background="@color/white"
        android:flipInterval="30"
        android:persistentDrawingCache="animation" />

    <View
        android:layout_width="wrap_content"
        android:layout_height="0.5dp"
        android:background="@color/white" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/main_line_height"
        android:layout_gravity="bottom"
        android:background="@color/white"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/cursor"
            android:layout_width="@dimen/main_matrix_width"
            android:layout_height="@dimen/main_line_height"
            android:scaleType="matrix"
            android:src="@color/blue" />
    </LinearLayout>

    <RadioGroup
        android:id="@+id/rg_bottom_tag"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#11000000"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:padding="3dp">

        <RadioButton
        android:id="@+id/rb_video"
        style="@style/tv_video"
        android:drawableTop="@drawable/tv_video_drawable_selector"
        android:text="@string/video" />

        <RadioButton
            android:id="@+id/rb_audio"
            style="@style/tv_video"
            android:drawableTop="@drawable/tv_audio_drawable_selector"
            android:text="@string/audio" />

        <RadioButton
            android:id="@+id/rb_dynamic"
            style="@style/tv_video"
            android:drawableTop="@drawable/tv_dynamic_drawable_selector"
            android:text="@string/dynamic" />
    </RadioGroup>
</LinearLayout>